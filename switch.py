"""Module that implements a functionality similar to the switch/case functionality in C"""


class switch(object):
    value = None

    def __new__(class_, value):
        class_.value = value
        return True


def case(*args):
    return any((arg == switch.value for arg in args))
